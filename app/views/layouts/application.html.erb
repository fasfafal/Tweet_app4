<!DOCTYPE html>
<html>
  <head>
    <title>TweetApp4</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <header>
    <div><%= link_to "トップ", root_path %></div>
   <% if user_signed_in? %>
   <div><%= link_to "投稿一覧", posts_path %></div>
   <div><%= link_to "新規投稿", new_post_path %></div>
   <div><%= link_to "ログアウト", destroy_user_session_path,  data:{turbo_method: :delete}%>
   <% else %>
     <div><%= link_to "ログイン", user_session_path %></div>
     <div><%= link_to "新規登録", new_user_registration_path %></div>
   <% end %> 
 </header>
 <body>
    <p class = "notice"><%= notice %></p>
    <p class = "alert"><%= alert %></p>
  
    <%= yield %>
  </body>
</html>
